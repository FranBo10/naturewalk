{% for label, messages in app.flashes('success') %} {% for message in messages
%}
<div class="alert alert-{{ label }} text-center w-50 mx-auto my-2">
  <p>{{ message | raw }}</p>
</div>
{% endfor %} {% endfor %}
