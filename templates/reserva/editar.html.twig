{% extends 'base.html.twig' %} {% block title %} Edit booking {% endblock %} {%
  block body %}
  <div id="main-container" class="container-fluid my-3">
    {% for message in app.flashes('danger') %}
    <div class="alert alert-danger text-center w-50 mx-auto my-2">
      <p>{{ message }}</p>
    </div>
    {% endfor %}
    <section class="table__section">
      <table class="table">
        <thead class="table__head">
          <tr>
            <th class="align-middle">Tour</th>
            <th class="align-middle">Price</th>
            <th class="align-middle">Adults</th>
            <th class="align-middle">Kids</th>
            <th class="align-middle">Booking date</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td class="text-center align-middle">
              <img
                src="{{ asset('uploads/images/' ~ tour.imagen) }}"
                alt="{{ tour.titulo }}"
                style="object-fit: cover"
                width="100%"
                height="100"
              />
              <div class="table__titulo">
                {{ tour.titulo | title }} : {{ tour.horaInicio|date("H:i") }} h -
                {{ tour.horaFin|date("H:i") }} h
              </div>
            </td>
            <td class="text-center align-middle">
              {% if tour.precio == 0 %}
              <p><span>Free Tour</span></p>
              {% else %}
              <p>{{ tour.precio/100|number_format(2, '.') }} €</p>
              {% endif %}
            </td>
  
            {% if app.user %}
            {{
              form_start(form, { attr: { novalidate: "novalidate" } })
            }}
            <td class="text-center align-middle">
              {{ form_row(form.cantidad_adultos) }}
            </td>
  
            <td class="text-center align-middle">
              {{ form_row(form.cantidad_kids) }}
            </td>
            <td class="text-center align-middle">
              {{ form_row(form.fecha_evento, {'attr': {'name': 'fechaEvento'}}) }}
            </td>
          </tr>
        </tbody>
      </table>
    </section>
  
    <div class="row col-12">
      <div class="col-6">
        <div class="buttons">
          <a href="{{ path('home') }}" class="btn-secondary me-3">
            Clear Cart
          </a>
          <a href="{{ path('home') }}" class="btn-tertiary">More tours...</a>
        </div>
      </div>
      <div class="col-6 text-end">
        <button type="submit" class="btn-primary me-3">Modify reservation</button>
        {{ form_end(form) }}
      </div>
    </div>
    <!-- Fin del bloque de formulario de datos personales -->
    {% else %}
    <p>No booking registered yet</p>
    {% endif %}
  </div>
  {% endblock %}
  