{% extends 'base.html.twig' %} {% block title %}Reset your password{% endblock
%} {% block body %}
<div id="main-container" class="container my-5">
  {% for flash_error in app.flashes('reset_password_error') %}
  <div class="alert alert-danger" role="alert">
    <p>{{ flash_error }}</p>
  </div>
  {% endfor %}
  <h2>Restablecer contraseña</h2>

  {{ form_start(requestForm) }}
  {{ form_row(requestForm.email) }}
  <div>
    <small>
      Introduce tu e-mail y te enviaremos un enlace para restablecer tu
      contraseña
    </small>
  </div>

  <button class="btn-primary mt-3">Restablecer contraseña</button>
  {{ form_end(requestForm) }}
</div>
{% endblock %}
