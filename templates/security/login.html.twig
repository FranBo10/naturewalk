{% extends 'base.html.twig' %} {% block title %} Log in {% endblock %} {% block
header %} {% include '_partials/_menu2_nav.html.twig' %} {% endblock %} {% block
body %} {# {% include 'app/_side_menu.html.twig' %} #}

<div id="main-container" class="container mt-3 d-flex justify-content-center">
  <form method="post" class="w-100">
    {% if app.user %}
    <div class="mb-3">
      Welcome {{ app.user.nombre }},
      <a href="{{ path('app_logout') }}">Log out</a>
    </div>
    {% endif %}
    <h1 class="text-center h3 my-3 pb-3 border-dark">Log in</h1>
    {% for message in app.flashes('success') %}
    <div class="alert alert-success text-center w-50 mx-auto my-2">
      <p>{{ message }}</p>
    </div>
    {% endfor %} {% if error %}
    <div class="alert alert-danger text-center w-50 mx-auto my-2">
      {{ error.messageKey|trans(error.messageData, 'security') }}
    </div>
    {% endif %}

    <label for="inputEmail">Email</label>
    <input
      type="email"
      value="{{ last_username }}"
      name="email"
      id="inputEmail"
      class="form-control mb-3"
      autocomplete="email"
      required
      autofocus
    />
    <label for="inputPassword">Password</label>
    <input
      type="password"
      name="password"
      id="inputPassword"
      class="form-control"
      autocomplete="current-password"
      required
    />
    <input
      type="hidden"
      name="_csrf_token"
      value="{{ csrf_token('authenticate') }}"
    />

    {# Uncomment this section and add a remember_me option below your firewall
    to activate remember me functionality. See
    https://symfony.com/doc/current/security/remember_me.html

    <div class="checkbox mb-3">
      <label> <input type="checkbox" name="_remember_me" /> Remember me </label>
    </div>
    #}

    <div class="menu__buttons">
      <button class="btn-primary mt-3" type="submit">Log in</button>
      <div class="mt-3">
        <a href="{{ path('app_forgot_password_request') }}"
          >I forgot my password</a
        >
      </div>
      <p class="mt-3">
        You still have no account ?
        <span>
          <a
            href="{{ path('registro_tratar') }}"
            data-bs-toggle="modal"
            data-bs-target="#registro"
            >Register here</a
          >
        </span>
      </p>
    </div>
  </form>
</div>
{% endblock %}
